source "https://rubygems.org"

# Matches what GitHub Pages builds with server-side. Local `bundle exec jekyll serve`
# will use this gem; remote_theme in _config.yml is resolved automatically by GitHub
# Pages in production, no separate Action/workflow needed.
gem "github-pages", group: :jekyll_plugins
gem "webrick" # required for `jekyll serve` on Ruby 3+
